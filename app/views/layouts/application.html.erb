<!DOCTYPE html>
<html>
<head>
  <title>NinetyNineCatsDay1</title>
  <% if current_user  %>
    <ul>
      <li>Logged in as: <%= current_user.user_name %></li>
      <li><%= button_to "Logout", session_url, method: :delete %></li>
    </ul>
    <% else %>
    <p>Not logged in, login or register</p>
    <p><%= button_to "Login", new_session_url, method: :get %></p>
    <p><%= button_to "Register", new_user_url, method: :get %></p>
  <% end %>


  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<%= yield %>

</body>
</html>
